import { StyleSheet } from "react-native";

const font = StyleSheet.create({
  white: {
    color: "#FFFFFF"
  },
  dark: {
    color: "#212121"
  },
  center: {
    textAlign: "center"
  }
});

export default font;
